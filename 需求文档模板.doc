<html xmlns:o="urn:schemas-microsoft-com:office:office"
      xmlns:w="urn:schemas-microsoft-com:office:word"
      xmlns="http://www.w3.org/TR/REC-html40">
<head>
<meta charset="utf-8">
<title>产品需求文档(PRD)标准模板</title>
<style>
    body { font-family: "Microsoft YaHei", sans-serif; line-height: 1.6; max-width: 900px; margin: 0 auto; padding: 24px; }
    h1 { text-align: center; font-size: 24pt; margin-bottom: 20px; }
    h2 { font-size: 16pt; color: #333; border-bottom: 1px solid #ccc; padding-bottom: 5px; margin-top: 30px; }
    h3 { font-size: 14pt; color: #444; margin-top: 20px; }
    h4 { font-size: 12pt; color: #555; margin-top: 16px; }
    table { border-collapse: collapse; width: 100%; margin-bottom: 20px; }
    th, td { border: 1px solid #000; padding: 8px; text-align: left; vertical-align: top; }
    th { background-color: #f2f2f2; }
    p { margin-bottom: 10px; }
    ul { margin-bottom: 10px; padding-left: 24px; }
    ul ul { margin-top: 6px; margin-bottom: 6px; }
    li { margin-bottom: 5px; }
    .summary { color: #333; }
    .hint { color: #666; font-style: italic; }
    .placeholder { color: #888; font-style: italic; }
    .screenshot { display: block; max-width: 100%; border: 1px solid #ccc; margin: 12px auto; }
    figcaption { color: #666; font-size: 10pt; text-align: center; margin-bottom: 16px; }
</style>
</head>
<body>

<h1>[模块名] — 产品需求文档 (PRD)</h1>
<p class="hint" style="background:#f8fafc;border:1px solid #e2e8f0;padding:12px;border-radius:6px;"><strong>维护说明：</strong>工具脚本、图示目录、写作规范、计费要求及 Markdown 源稿模板，统一见仓库根目录 <code>PRD编写与工具说明.md</code>。本文档仅作 Word 章节骨架；定稿建议以 <code>admin/docs/[模块]需求文档.md</code> 为准并导出 docx。</p>

<h2>1. 文档说明</h2>
<p class="summary">用一句话说明本模块做什么（例：明确「XX 模块」的核心流程、关键规则及与周边系统的衔接）。</p>

<h3>1.1 修订记录</h3>
<table>
    <tr>
        <th>版本号</th>
        <th>修订日期</th>
        <th>修订人</th>
        <th>修订说明</th>
    </tr>
    <tr>
        <td>V1.0.0</td>
        <td>2026-XX-XX</td>
        <td>你的名字</td>
        <td>初始版本创建</td>
    </tr>
</table>

<h3>1.2 名词解释</h3>
<p class="hint">只列术语表，不写筛选说明。解释用业务语言，不写数据库字段名、绑定键、URL 等；详见 <code>PRD编写与工具说明.md</code> §4.1。</p>
<table>
    <tr>
        <th>专有名词</th>
        <th>解释/定义</th>
    </tr>
    <tr>
        <td>（示例）生效中</td>
        <td>说明该状态在本系统中的判定条件及关联行为（如是否只读）</td>
    </tr>
</table>

<h2>2. 产品概述</h2>
<h3>2.1 产品背景</h3>
<p>为什么要做、解决什么痛点；与上下游业务的关系。</p>

<h3>2.2 目标用户</h3>
<table>
    <tr>
        <th>用户角色</th>
        <th>特征与诉求</th>
    </tr>
    <tr>
        <td>运营 / 商务</td>
        <td>（填写）</td>
    </tr>
    <tr>
        <td>研发 / 测试</td>
        <td>（按需填写，可合并为一行）</td>
    </tr>
</table>

<h3>2.3 产品目标</h3>
<table>
    <tr>
        <th>类型</th>
        <th>目标说明</th>
    </tr>
    <tr>
        <td>定性</td>
        <td>（填写）</td>
    </tr>
    <tr>
        <td>定量</td>
        <td>（填写，可量化指标）</td>
    </tr>
</table>
<p><strong>本期不包含：</strong></p>
<ul>
    <li>（明确排除范围，避免范围蔓延）</li>
</ul>

<h3>2.4 关联模块</h3>
<table>
    <tr>
        <th>关联模块</th>
        <th>与本模块关系</th>
    </tr>
    <tr>
        <td>（模块 A）</td>
        <td>数据从哪来、结果写到哪、触发时机</td>
    </tr>
</table>

<h2>3. 业务流程与架构</h2>
<p class="hint" style="color:#999;font-size:9pt;">（图示导出与脚本见 <code>PRD编写与工具说明.md</code>，勿写入正式 PRD 正文。）</p>

<h3>3.1 核心业务流程图</h3>
<p class="hint">复杂匹配、核销、状态流转等须用流程图表达；正文中<strong>插入流程图图片</strong>，图下补充匹配顺序、分支规则等文字说明。</p>
<p class="placeholder">[插入图片：admin/docs/images/[模块文件夹名]/05-核心业务流程.png]</p>
<p class="hint" style="text-align:center;">图 3.1 — [一句话说明：如匹配顺序、计费触发节点]</p>
<p><strong>文字补充（建议必填）：</strong></p>
<ol>
    <li>（示例）步骤或匹配优先级说明</li>
    <li>（示例）异常/分支说明</li>
</ol>

<h3>3.2 页面流转图</h3>
<p class="hint"><strong>维护说明（勿写入正式 PRD 标题）：</strong>本节可选；无多页跳转关系时可整节省略。</p>
<p>主要页面路径；注明原型路径，如 <code>admin/pages/xxx.html</code> → <code>xxx-edit.html</code>。</p>
<p class="placeholder">[插入图片：admin/docs/images/[模块文件夹名]/06-页面流转图.png]</p>
<p class="hint" style="text-align:center;">图 3.2 — [列表 / 编辑 / 弹窗之间的跳转关系]</p>

<h2>4. 需求详细说明</h2>

<h3>4.1 产品方案概述</h3>
<p>用若干条概括端到端方案（维护 → 匹配/流转 → 核心处理 → 输出/落库）：</p>
<ol>
    <li><strong>（环节 1）：</strong>简述</li>
    <li><strong>（环节 2）：</strong>简述</li>
    <li><strong>（环节 3）：</strong>简述</li>
</ol>

<h3>4.2 核心功能一览</h3>
<p class="hint">先让读者抓住「四大核心能力」，再索引到 4.3；勿在 4.2 堆长清单表。</p>
<h4>4.2.1 四大核心能力</h4>
<table>
    <tr>
        <th>序号</th>
        <th>核心能力</th>
        <th>解决什么问题</th>
        <th>关键功能点（P0）</th>
    </tr>
    <tr>
        <td>①</td>
        <td><strong>（能力名）</strong></td>
        <td>…</td>
        <td>…</td>
    </tr>
</table>
<h4>4.2.2 核心功能点索引</h4>
<table>
    <tr>
        <th>核心功能点</th>
        <th>说明摘要</th>
        <th>详述章节</th>
    </tr>
    <tr>
        <td>示例功能点</td>
        <td>一句话</td>
        <td><strong>4.3.1</strong></td>
    </tr>
</table>
<h4>4.2.3 其他功能</h4>
<table>
    <tr><th>功能</th><th>说明</th></tr>
    <tr><td>示例</td><td>…</td></tr>
</table>

<h3>4.3 具体功能详述</h3>
<p class="hint">按 4.2.2 索引展开；标题建议：<code>4.3.x 【模块名】功能名</code>。复杂规则可用表格补充。</p>
<p class="hint"><strong>操作流程：</strong>必须用 <strong>1. 2. 3.</strong> 编号体现顺序；多条业务路径用「路径一/路径二」分段；列表页等多入口先写进入与查询，再用表格写并行操作。勿把输入输出写进步骤里。详见 <code>PRD编写与工具说明.md</code> §4.5。</p>
<p class="hint"><strong>表述约定：</strong>正文与计算案例使用业务语言（价目类型、关联费用项、分区等），<strong>不要</strong>写字段名（如 region）、绑定键、URL、代码式条件。产品 PRD 不写【测试】验收标准、【研发】待实现专章。详见 <code>PRD编写与工具说明.md</code> §4.1。</p>
<p class="hint"><strong>计费模块：</strong>凡涉及报价、计费、费用生成、附加费、分摊，须单独增加「【计费引擎】计费规则」类章节，写清公式与触发条件，并为<strong>每种计价方式</strong>提供带数字的<strong>计算案例</strong>（计划实测 + 价目 + 公式 + 结果）。附加费须覆盖各计费方式及单询/拦截边界。</p>

<h4>4.3.1 【模块名】[功能名]</h4>
<ul>
    <li><strong>使用场景：</strong></li>
    <li><strong>前置条件：</strong></li>
    <li><strong>操作流程：</strong>（编号步骤；多路径见路径一/二；并行操作用表）
        <ol>
            <li><strong>（步骤名）：</strong> …</li>
            <li><strong>（步骤名）：</strong> …</li>
            <li><strong>选择后续操作</strong>（可选）：</li>
        </ol>
        <table>
            <tr><th>操作</th><th>说明</th><th>下一步</th></tr>
            <tr><td>示例</td><td>…</td><td>见 4.3.x</td></tr>
        </table>
    </li>
    <li><strong>数据输入与输出：</strong>
        <ul>
            <li><strong>输入：</strong></li>
            <li><strong>输出：</strong></li>
        </ul>
    </li>
    <li><strong>业务规则（核心）：</strong>
        <ul>
            <li><strong>字段校验：</strong></li>
            <li><strong>状态流转：</strong></li>
            <li><strong>权限控制：</strong></li>
            <li><strong>其他规则：</strong></li>
        </ul>
    </li>
    <li><strong>异常处理：</strong></li>
    <li><strong>界面截图（可选）：</strong>复杂交互可在此放 1～2 张图，或统一汇总到 <strong>5.3</strong> 按页面排列。</li>
</ul>

<h4>4.3.2 【模块名】[功能名]</h4>
<p class="placeholder">（复制 4.3.1 结构继续填写）</p>

<h4>4.3.x 【计费引擎】计费规则（涉及报价/计费时必填）</h4>
<ul>
    <li><strong>使用场景：</strong>（何时触发计费，如拆柜完成、下单匹配后等）</li>
    <li><strong>前置条件：</strong>（已匹配报价、路由到价目 Tab、实测字段齐全等）</li>
    <li><strong>业务规则（核心）：</strong>
        <ul>
            <li>按计价方式分条写：公式、取价维度、例外规则（超重、降级、拦截等）</li>
            <li>费用项：写应收费用项<strong>中文名</strong>及价目来源（如「拆柜-组合仓 Tab 基础提拆价格」），不写绑定键</li>
        </ul>
    </li>
    <li><strong>计算案例（说明用）：</strong>
        <ul>
            <li>每种计价方式至少 1 例：条件表（路由/计划/价目）+ 计算过程 + 结果金额</li>
            <li>注明：实际以报价单配置与业务实测为准；币种同报价单</li>
        </ul>
    </li>
    <li><strong>附加费（若有）：</strong>各计费方式（超出按量、固定加收、按天、双项固定、重量阶梯、超限拦截、单询）的规则表 + 计算案例表</li>
    <li><strong>异常处理：</strong>缺价目、超限拦截、单询待确认等</li>
</ul>

<h4>4.3.x 【报价编辑】附加费（若有）</h4>
<p class="placeholder">（触发条件 + 计费方式规则；须附计算案例表）</p>

<h2>5. 界面与交互需求</h2>
<h3>5.1 页面布局规范</h3>
<p>页面结构、组件约定；可写原型路径，如 <code>admin/pages/xxx.html</code>。</p>

<h3>5.2 交互反馈</h3>
<p>成功/失败提示、Loading、二次确认、只读态提示等；建议用表格按场景列出。</p>

<h3>5.3 功能界面截图</h3>
<p class="hint">按页面插入截图并写图题说明；不写仓库路径、导出脚本等维护说明。<strong>正式 PRD 小节标题勿写「（示例）」「（可选）」</strong>——价目 Tab 任选一类展示、状态/提示节可省略等，仅在本模板或 <code>PRD编写与工具说明.md</code> §4.3.1 标注。</p>

<h4>5.3.1 [页面名称，如：列表页]</h4>
<p class="placeholder">[插入截图：页面全貌，含查询区 / 表格 / 主要按钮]</p>
<p class="hint" style="text-align:center;">图 5.3.1 — [一句话说明图中标注重点]</p>

<h4>5.3.2 [页面名称，如：编辑页]</h4>
<p class="placeholder">[插入截图：关键 Tab 或表单区域]</p>
<p class="hint" style="text-align:center;">图 5.3.2 — [说明]</p>

<h4>5.3.3 [页面名称，如：价目 Tab 或弹窗]</h4>
<p class="hint"><strong>维护说明：</strong>价目类截图任选一种价目类型展示即可（正式标题不写「示例」）。</p>
<h4>5.3.4 [关键状态与提示]</h4>
<p class="hint"><strong>维护说明：</strong>本节可选；无独立状态页可在 5.3.2 图题中说明只读/报错即可。</p>
<p class="placeholder">[插入截图：只读态、二次确认、报错 Toast 等]</p>
<p class="hint" style="text-align:center;">图 5.3.3 — [说明]</p>

<h2>6. 非功能性需求</h2>
<h3>6.1 性能需求</h3>
<p>（填写）</p>
<h3>6.2 安全需求</h3>
<p>权限、审计、脱敏、接口校验等。</p>
<h3>6.3 兼容性需求</h3>
<p>浏览器、分辨率、终端类型（PC / PDA 等）。</p>

<h2>7. 数据统计与埋点需求</h2>
<h3>7.1 业务数据监控</h3>
<h3>7.2 埋点需求清单</h3>

<h2>8. 上线与运维计划</h2>
<ul>
    <li><strong>分阶段计划：</strong>一期 / 二期范围（若有）</li>
    <li><strong>数据初始化：</strong>上线前数据、配置、脚本</li>
    <li><strong>培训与支持：</strong>运营说明、手册、模板</li>
    <li><strong>回滚策略：</strong>异常时如何恢复（若有）</li>
</ul>

<h2>附录</h2>
<p class="hint"><strong>维护说明：</strong>附录整章可选；正式 PRD 若有附录，标题写「附录」即可。路由表、费用项与价目来源等<strong>业务速查</strong>放此处（用中文费用项名，不写绑定键）。Mermaid 源码、导出脚本见 <code>PRD编写与工具说明.md</code>。</p>
<table>
    <tr>
        <th>（列 1）</th>
        <th>（列 2）</th>
    </tr>
    <tr>
        <td></td>
        <td></td>
    </tr>
</table>

</body>
</html>
