# 2026-08-20

## PDA docs-shipment.html 页面分析

- 分析了 `PDA/docs-shipment.html` 货件详情页，含页面结构和数据解析流程
- 数据层在 `PDA/pda-docs-shared.js`，含 PALLETS 数组（6条托盘）和 SHIPMENT_META（3条货件元数据：CRN-2026-018/770018/660092）
- `q=CRN-2026-123` 走演示回退路径：未命中真实数据 → buildDemoShipment 生成 6 个虚拟托盘 + defaultDemoMeta 注入默认元数据 → enrichShipment 组装 → renderDetail 渲染
- 关键设计点：演示回退机制导致页面永远不会显示「未找到」；里程碑状态通过 milestoneState() 推断三态（done/current/pending）；XSS 通过 escapeHtml 防护
- 真实数据仅 3 条 REF，要查看真实场景需用 018/770018/660092 作为 q 参数

## PDA docs-shipment.html 改造方案

- 用户要求对该页面进行改造，业务需求未定，需自主设计
- 基于 3 个核心问题（数据层假 / 纯只读 / 孤岛页）+ PDA 7 大使用场景，设计了 4 阶段渐进式改造方案
- 方案已写入 `PDA/docs-shipment-改造方案.md`（331 行）
- Phase 1 数据层重构（去 demo 回退 + API 适配器 + 三态完备）
- Phase 2 操作层建设（底部动作栏 + 状态驱动按钮 + 流程页跳转）
- Phase 3 视图层优化（板标可点击 + 搜索排序 + Tab 持久化 + 复制 + 超期提醒 + 异常独立 Tab）
- Phase 4 导航与集成（返回来源感知 + 工单号跳转 + 拆分子单跳转 + 扫码入口）
- 优先级：P0（Phase 1+2 核心）→ P1（导航打通）→ P2（体验提升）→ P3（硬件依赖）
- 核心约束：保持单文件架构，不引入框架，渐进式增强，`?mock=1` 向后兼容
