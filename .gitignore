# 主要用途：告诉 Git 哪些文件/目录不要纳入版本库（本地仍保留，仅不提交）
.DS_Store
*.log
# .claude/ — Claude Code 本地权限与设置（不用 Claude Code 可删）
.claude/
# .continue/ — Continue 插件示例配置（不用 Continue 可删）
.continue/
# .cursor/ — Cursor 项目规则与调试日志（rules 有用；debug-*.log 可随时删）
.cursor/
# 忽略系统模板文件夹及下属所有内容
**/系统模板/**
# 忽略项目资料文件夹及下属所有内容
**/项目资料/**

# .probe-pw/ — 本地 Playwright 探测沙箱（非正式 UI，不用可整目录删）
.probe-pw/
node_modules/