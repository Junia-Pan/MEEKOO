# PRD 编写与工具说明

> **本文档唯一入口**：产品、AI、文档维护人查阅 PRD 规范、目录约定、脚本用法与章节模板。  
> **不要**将本文内容复制进 `admin/docs/*需求文档.md` 或对外 Word 正文。  
> AI 约束另见 `.cursor/rules/prd-formal-output.mdc`。

---

## 1. 文档分工

| 文件 | 用途 |
|------|------|
| `admin/docs/[模块]需求文档.md` | **正式 PRD 正文**（源稿，给业务/评审） |
| `admin/docs/[模块]需求文档.docx` | 由 md 导出，便于 Word 分发 |
| **本文** `PRD编写与工具说明.md` | 工具、目录、写作规范、章节模板（仓库根目录） |
| `需求文档模板.doc` | 可选：用 Word 起稿时的章节骨架（与 **附录 A** 等价；已含 **§4.7** 界面精简说明） |
| `admin/docs/diagrams/[模块]/*.mmd` | 流程图 Mermaid 源码 |
| `admin/docs/images/[模块]/` | 截图与渲染后的流程图 PNG |
| `admin/docs/_*.py` | 截图、渲染、导出脚本（留在 `admin/docs/`，路径不变） |

**推荐流程：** 复制已有模块 md 或按 **附录 A** 新建 → 填正文 → 改图/截图 → 运行工具链 → 得到 docx。

---

## 2. 工具脚本

均在仓库根目录执行。依赖：**Python 3**；`_render_mermaid.py`、`_capture_prd_screenshots.py` 需 **Playwright + Chromium**（首次可执行 `playwright install chromium`）。

### 2.1 推荐执行顺序

```bash
# 1）流程图：.mmd → PNG（第 3 章用）
python admin/docs/_render_mermaid.py --module 提拆派报价

# 2）界面截图：原型页 → PNG（第 5.3 章用，按模块配置页面列表）
python admin/docs/_capture_prd_screenshots.py

# 3）正式稿：md → docx（导出前请关闭 Word 中的目标 docx）
python admin/docs/_md_to_docx.py
```

### 2.2 `_render_mermaid.py`

| 项 | 说明 |
|----|------|
| 作用 | 将 `admin/docs/diagrams/[模块]/` 下 `.mmd` 渲染为 PNG |
| 默认输出 | `admin/docs/images/提拆派报价/05-核心业务流程.png`、`06-页面流转图.png` |
| 用法 | `python admin/docs/_render_mermaid.py` 或 `--module 模块文件夹名` |
| 注意 | 改 `.mmd` 后须重跑，再执行 `_md_to_docx.py` |

### 2.3 `_capture_prd_screenshots.py`

| 项 | 说明 |
|----|------|
| 作用 | 从 `admin/pages/` 原型 HTML 自动截图 |
| 默认输出 | `admin/docs/images/提拆派报价/01-列表页.png` 等 |
| 用法 | `python admin/docs/_capture_prd_screenshots.py`（脚本内配置模块与页面） |
| 注意 | 本地起静态服务后截图；完成后执行 `_md_to_docx.py` |

### 2.4 `_md_to_docx.py`

| 项 | 说明 |
|----|------|
| 作用 | 将指定 md 转为 Word |
| 默认 | `admin/docs/提拆派报价需求文档.md` → 同目录 `.docx` |
| 支持 | 标题、表格、列表、加粗、`![说明](images/模块/xx.png)` 插图 |
| 注意 | 第 3 章须已存在 PNG；若 docx 被 Word 占用，会保留 `._gen.docx` 临时文件，关闭 Word 后重跑即可覆盖正式 docx |

扩展新模块时：在对应脚本内增加模块名/路径配置，或复制现有模块块后改文件名。

---

## 3. 图示与目录约定

- 正式 PRD **第 3 章、5.3 节**在 Word 中须为 **PNG/JPG**，勿仅在正文保留 Mermaid 代码块。
- **界面截图**：`admin/docs/images/[模块名]/01-`～`04-`
- **流程图**：`05-核心业务流程.png`、`06-页面流转图.png`
- **Mermaid 源码**：`admin/docs/diagrams/[模块名]/*.mmd`（不写入正式 PRD 附录）

---

## 4. 写作规范（正式 PRD 正文）

### 4.1 业务表述

- 面向业务、运营、评审；正文与**计算案例**用中文业务术语。
- **名词解释**：只保留术语表，**不要**写「仅收录非字面可懂…」等筛选说明段落。
- **不要**出现下列开发向写法：
  - 数据库/接口字段名（如 `region`、绑定键 `unpackBase`）
  - URL、查询参数（如 `?copy=`）、仓库文件路径（如 `admin/pages/xxx.html`）
  - 代码式条件（如 `` `业务类型 = 拆柜` ``、`Move Type ∈ {…}`）
  - 「字段」「字段级」「按实现约定」「技术方案/后端评估」等研发口吻
- **宜写**：业务类型为「拆柜」、关联应收费用项、价目类型、价目表中的分区、表单项校验等。
- 费用出账写清：**应收费用项名称** + **价目来源**（如「拆柜-组合仓 基础提拆价格」）。
- 绑定键、接口字段、枚举**不写入**正式 PRD（由研发单独约定）。

### 4.2 计费章节（涉及报价/计费/附加费时必填）

在 4.3 中单独成节（如【计费引擎】计费规则），须包含：

1. **业务规则**：公式、触发条件、取价维度、例外（超重、降级、拦截、单询等）。
2. **计算案例**：每种计价方式 ≥1 组数字演算（条件 + 价目 + 公式 + 结果）；注明以报价单配置与实测为准。
3. **附加费**：各计费方式（超出按量、固定加收、重量阶梯、单询）均需规则 + 案例。

功能描述与计算案例用**费用项中文名**，不写绑定键。

### 4.3 不得写入正式 PRD 的内容

- 本文、脚本命令、仓库路径维护说明、「改图后请运行」类提示。
- 附录中的 Mermaid 源码块（源码只留在 `diagrams/`）。
- 章节标题或导航中的编写提示：**（可选）**、**（示例）**、**（本期必看）**、**（P1，…）** 等——仅写在本文或 `需求文档模板.doc` 中标注哪些节可省略、哪张截图作代表即可；正式 PRD 用正式章节名（如 `3.2 页面流转图`、`5.3.3 报价编辑页 — 价目`）。步骤里说明非必做操作用 **「按需」**，勿写 **「（可选）」**。

### 4.3.1 可选用章节（仅维护文档标注）

| 章节 | 维护文档中的标注 | 正式 PRD 标题写法 |
|------|------------------|-------------------|
| 3.2 页面流转图 | 可选：无多页跳转时可省略整节 | `### 3.2 页面流转图` |
| 5.3 部分截图 | 示例：价目 Tab 任选一类展示即可 | `#### 5.3.3 报价编辑页 — 价目`（不写「示例」） |
| 5.3.4 状态与提示 | 可选：无独立状态页可并入 5.3.2 图题说明 | `#### 5.3.4 关键状态与提示` |
| 附录 | 可选 | 有附录再建章，标题不写「（可选）」 |

### 4.4 第 7 章（数据统计与埋点）

- 模板保留 **第 7 章** 及 **7.1 / 7.2** 小节标题；**正文留空**，不写「预留」「暂不输出」等提示语。
- **不要**主动编写指标表、埋点事件表；产品明确要求后再补充。

### 4.5 操作流程（4.3 功能详述）

4.3 各节的 **操作流程** 须让读者一眼看出**先后顺序**，写法如下：

1. **用编号步骤**：主流程写 `1. 2. 3. …`，勿用无序要点堆砌同级动作。
2. **多路径分开写**：互斥或可选业务路径用 **路径一 / 路径二**（或 **主流程 + 分支**）分段，每段内仍用编号。
3. **列表页等多入口**：先写「进入 → 按需查询」等公共步骤，再写 **选择后续操作**；并行操作用表格列「操作 | 说明 | 下一步」。
4. **系统自动化**：按 **触发事件 → 读取/判断 → 处理 → 输出/落库** 顺序编号。
5. **独立小节**：「数据输入与输出」「业务规则」「异常处理」**不要**塞进操作流程步骤里。

### 4.6 修订记录

正式 PRD **§1.1 修订记录** 默认只保留一行 **初稿**（版本 V1.0、日期、说明「初稿」）。**不要**在撰写或优化 PRD 时自动追加 V1.1、V2.0 等版本行；仅当产品**明确要求输出/更新修订记录**时再填写后续版本及说明。

### 4.7 界面说明与原型（精简原则）

本仓库模块多有 **HTML 原型页**（`admin/pages/`）。正式 PRD 与原型分工如下：

| 写什么 | 不写什么 |
|--------|----------|
| 原型**看不出**的规则：状态机、跳步/禁止、校验、回退清理、权限、跨模块联动 | 原型上**一眼能懂**的布局：Tab 名称、按钮文案、弹窗标题、表单项标签、取消/确认等 |
| 业务规则用 **§3 / §4.3 业务规则** 表格或条目 | 把原型界面「翻译」成长段操作流程或「数据输入与输出」复述 |
| 界面章节点到为止（见下） | 与业务规则重复的「速查表」「校验汇总」拆成多章 |

**界面章节（第 5 章或「界面说明」）推荐写法：**

1. 章首一句：**界面以现有原型为准；本节仅列与业务相关的要点。**
2. 按页面/弹窗分小节，每节 **3～6 条短要点**（列表页、弹窗、详情各一节即可）。
3. 有截图时：**图 + 图题** 为主，文字只补业务约束（如「批量须先勾选」「回退原因必填」）。
4. 无独立原型、或原型未覆盖的分支，才在正文用编号步骤写清。

**参考示例：** `admin/docs/外仓收货-状态变更与回退.md`（业务规则集中、界面章节约一页要点）。

**AI / 作者自检：** 读者打开原型能直接看到的，删出 PRD；删完后若界面章节约占全文一半以上，通常仍偏长，应再合并进业务规则表。

编写原则（如「原型能看懂的不写」）**不写入**正式 PRD 正文与修订说明，见 **§4.7** 与 `.cursor/rules/prd-formal-output.mdc`。

---

## 附录 A：需求文档章节模板（Markdown）

新建模块时，复制以下结构为 `admin/docs/[模块名]需求文档.md`，删除占位说明后填写。

```markdown
# [模块名] — 产品需求文档 (PRD)

## 1. 文档说明

（一句话说明本模块做什么。）

### 1.1 修订记录

| 版本号 | 修订日期 | 修订人 | 修订说明 |
|--------|----------|--------|----------|
| V1.0 | YYYY-MM-DD | — | 初稿 |

### 1.2 名词解释

| 专有名词 | 解释/定义 |
|----------|-----------|
| （本模块特有术语，用业务语言解释） | |

---

## 2. 产品概述

### 2.1 产品背景

### 2.2 目标用户

| 用户角色 | 特征与诉求 |
|----------|------------|

### 2.3 产品目标

| 类型 | 目标说明 |
|------|----------|
| 定性 | |
| 定量 | |

**本期不包含：**

- 

### 2.4 关联模块

| 关联模块 | 与本模块关系 |
|----------|--------------|

---

## 3. 业务流程与架构

### 3.1 核心业务流程图

![核心业务流程](images/[模块名]/05-核心业务流程.png)

*图 3.1 — （匹配顺序、分支规则等文字说明）*

### 3.2 页面流转图

> **维护说明（勿写入正式 PRD 标题）：** 本节可选；无多页跳转时可省略。

![页面流转图](images/[模块名]/06-页面流转图.png)

*图 3.2 — （页面跳转关系）*

---

## 4. 需求详细说明

### 4.1 产品方案概述

1. 
2. 

### 4.2 核心功能一览

> 先写「四大核心能力」表（能力名 + 解决什么问题 + P0 功能点），再写「核心功能点索引」表（功能点 → 4.3.x 章节）。细项清单可放 4.2.3（P1 等），避免 4.2 变成难扫的长表。

#### 4.2.1 四大核心能力

| 序号 | 核心能力 | 解决什么问题 | 关键功能点（P0） |
|:----:|----------|--------------|------------------|
| ① | **（能力名）** | … | … |

#### 4.2.2 核心功能点索引

| 核心功能点 | 说明摘要 | 详述章节 |
|------------|----------|----------|
| … | … | **4.3.x** |

#### 4.2.3 其他功能

| 功能 | 说明 |
|------|------|

### 4.3 具体功能详述

#### 4.3.1 【模块名】功能名

- **使用场景：**
- **前置条件：**
- **操作流程：**（须 **1. 2. 3.** 编号；多路径用「路径一/二」；列表页分支可用表格，见本文 **§4.5**）
  1. **（步骤名）：** …
  2. **（步骤名）：** …
  3. **选择后续操作**（并行操作用表；非必做步骤写「按需」勿写「可选」）：

| 操作 | 说明 | 下一步 |
|------|------|--------|
| … | … | … |

- **数据输入与输出：**
- **业务规则（核心）：**
- **异常处理：**

（按 4.2.2 索引展开 4.3.2、4.3.3…）

#### 4.3.x 【计费引擎】计费规则（涉及报价/计费时必填）

- **使用场景：**
- **前置条件：**
- **业务规则（核心）：**（公式、取价维度、例外）
- **计算案例（说明用）：**（每种计价方式至少 1 例）
- **异常处理：**

---

## 5. 界面与交互需求

> **维护说明（勿写入正式 PRD）：** 有原型时遵守本文 **§4.7** — 原型能一眼看懂的不要赘述；第 5 章以截图 + 短要点 + 业务约束为主。

### 5.0 界面要点（有原型时推荐）

（按列表页 / 弹窗 / 详情分小节，每节 3～6 条短要点；不写 Tab 名称、按钮文案等原型已展示内容。）

### 5.1 页面布局规范

（无原型或布局有特殊约束时再写；否则写「以原型为准」即可。）

### 5.2 交互反馈

（只写原型看不出的：须先勾选、必填、二次确认文案、拦截原因等。）

| 场景 | 交互要求 |
|------|----------|

### 5.3 功能界面截图

> **维护说明：** 正式 PRD 标题勿写「（示例）」「（可选）」；见本文 §4.3.1、§4.6。

#### 5.3.1 列表页

![列表页](images/[模块名]/01-列表页.png)

*图 5.3.1 — *

#### 5.3.3 编辑页 — 价目

（价目 Tab 任选一类价目截图即可。）

#### 5.3.4 关键状态与提示

（无独立状态页可省略本节。）

---

## 6. 非功能性需求

### 6.1 性能需求
### 6.2 安全需求
### 6.3 兼容性需求

---

## 7. 数据统计与埋点需求

### 7.1 业务数据监控

### 7.2 埋点需求清单

---

## 8. 上线与运维计划

- **数据初始化：**
- **培训与支持：**
- **回滚策略：**

---

## 附录

> **维护说明：** 附录整章可选；正式 PRD 标题写「附录」即可。

（业务速查表；不写绑定键、脚本路径。）
```

---

## 附录 B：与 Word 模板的关系

- 需用 **Microsoft Word** 空白起稿时，可打开 `需求文档模板.doc`（与本文件同级），章节与 **附录 A** 一致；模板已同步 **§4.6**（界面以原型为准、不赘述一眼能懂的内容）。
- 定稿仍建议以 **md 为源稿**，用 `_md_to_docx.py` 导出，便于版本管理与 AI 协作。
